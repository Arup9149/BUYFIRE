{
  "name": "buyfire",
  "version": "1.0.0",
  "private": true,
  "description": "BUYFIRE — Attack your store with AI buyers before real AI buyers do.",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "cd shared && npm run build && cd ../backend && npm run build && cd ../frontend && npm run build",
    "test": "cd backend && npm test",
    "start": "cd backend && npm start",
    "install:all": "npm install && cd shared && npm install && cd ../backend && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
